const Header = () => {
    return (
        <header>
            <nav>
                <button value={'Login'} />
                <button value={'Register'} />
                <button value={'Dashboard'} />
                <button value={'Account'} />
            </nav>
        </header>
    );
};

export default Header;
